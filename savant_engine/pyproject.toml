[project]
name = "prosavant-engine"
version = "0.0.1"
description = "ProSavantEngine core"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["prosavant_engine*"]
