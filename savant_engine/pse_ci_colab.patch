*** Begin Patch
*** Add File: .github/workflows/ci.yml
+name: ProSavantEngine CI
+
+on:
+  push:
+    branches: [ main, master ]
+  pull_request:
+    branches: [ main, master ]
+
+permissions:
+  contents: read
+
+jobs:
+  build-test:
+    runs-on: ubuntu-latest
+    timeout-minutes: 25
+    strategy:
+      fail-fast: false
+      matrix:
+        python-version: [ "3.10", "3.11" ]
+    steps:
+      - name: Checkout
+        uses: actions/checkout@v4
+
+      - name: Set up Python ${{ matrix.python-version }}
+        uses: actions/setup-python@v5
+        with:
+          python-version: ${{ matrix.python-version }}
+          cache: 'pip'
+          cache-dependency-path: |
+            requirements.txt
+
+      - name: Install dependencies
+        run: |
+          python -m pip install --upgrade pip
+          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
+          # minimal dev tooling (optional)
+          pip install --upgrade ruff pytest uvicorn
+
+      - name: Lint (ruff)
+        run: |
+          ruff --version || true
+          ruff check . || true
+
+      - name: Unit tests (if any)
+        run: |
+          if [ -d tests ] && ls tests/*.py >/dev/null 2>&1; then
+            pytest -q
+          else
+            echo "No tests/ found — skipping pytest."
+          fi
+
+      - name: API smoke test
+        env:
+          PORT: 8000
+        run: |
+          set -euxo pipefail
+          # Boot the app quickly and probe /health
+          python -c "import app"  # import sanity
+          uvicorn app:app --host 127.0.0.1 --port ${PORT} &
+          APP_PID=$!
+          # give it a moment
+          for i in 1 2 3 4 5; do
+            curl -sf "http://127.0.0.1:${PORT}/health" && break || sleep 1
+          done
+          echo; echo "Health:"; curl -s "http://127.0.0.1:${PORT}/health" | python -m json.tool || true
+          kill ${APP_PID} || true
+
*** End Patch
